import { Client, ClientOptions } from '@microsoft/teams.common/http';
import { ApiClientSettings } from './api-client-settings.mjs';
import { ChannelInfo } from '../models/channel-data/channel-info.mjs';
import { TeamDetails } from '../models/team-details.mjs';

declare class TeamClient {
    readonly serviceUrl: string;
    get http(): Client;
    set http(v: Client);
    protected _http: Client;
    protected _apiClientSettings: Partial<ApiClientSettings>;
    constructor(serviceUrl: string, options?: Client | ClientOptions, apiClientSettings?: Partial<ApiClientSettings>);
    getById(id: string): Promise<TeamDetails>;
    getConversations(id: string): Promise<ChannelInfo[]>;
}

export { TeamClient };
